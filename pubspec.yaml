name: bloc_demo
description: Example project that demonstrates implementing BLoc design pattern with Flutter
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^6.0.2
  flutter_bloc: ^6.0.3
  modal_progress_hud: ^0.1.3

flutter:
  uses-material-design: true